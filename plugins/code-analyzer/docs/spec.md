# Code Analyzer 共用规范

所有分析 Skill 生成文档时共同遵守的规范。

## 1. 公共输入参数

所有分析 Skill 接收以下公共参数：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `scope` | string | 否 | 分析范围（目录路径），默认为当前目录 `.` |
| `output_file` | string | 否 | 输出文件路径，由调用方指定 |

**输出文件统一放在 `./docs/code-analyzer/` 目录下。**

## 2. 忽略规则

如目标路径下存在 `.claudeignore` 文件，分析 Skill 必须在遍历目录树的**第一步**读取并遵循：

- 支持标准 `.gitignore` 格式（通配符 `*`、`**`、否定规则 `!` 等）
- 被忽略的目录在扫描时完全跳过（包括子目录）
- 被忽略的文件不参与分析和统计
- 如不存在 `.claudeignore`，正常执行，不忽略任何文件

## 3. 容错处理

所有分析 Skill 遵循统一的容错规则：

| 情况 | 处理方式 |
|------|----------|
| 目标路径不存在 | 终止分析，返回错误信息 |
| 目录为空或无代码文件 | 生成空报告，标注"无代码文件" |

## 4. 文档结构

### 头部（必须）

```markdown
# {文档标题}

> [返回 Manifest](./manifest.md)

## 导航

| 章节 | 链接 |
|------|------|
| {章节1} | [#1-{章节1}](#1-{章节1}) |
```

### 章节末尾

每个章节末尾添加返回导航链接：

```markdown
[↑ 返回导航](#导航)
```

### 底部（必须）

```markdown
---
*生成时间: {YYYY-MM-DD HH:MM}*

[↑ 返回导航](#导航)
```

### 文档间引用

使用相对路径：`[src-structure.md](./src-structure.md)`（文档统一放在 `./docs/code-analyzer/` 目录下）

## 5. 代码链接格式

```markdown
[显示文本](file_path:line_number)
```

- 使用相对于项目根目录的路径，不含前导 `./`
- 统一使用正斜杠 `/`（包括 Windows）
- 行号从 1 开始，可用范围格式 `path:45-78`

| 类型 | 格式 | 示例 |
|------|------|------|
| 方法 | `ClassName::Method` | `[GameManager::Init](src/manager.cpp:45)` |
| 函数 | `FunctionName` | `[LoadConfig](src/config.cpp:23)` |
| 类 | `ClassName` | `[GameObject](src/object.h:15)` |
| 成员 | `ClassName::member` | `[GameManager::m_render](src/manager.h:34)` |

## 6. 评分标准

### 通用维度（所有分析类型）

| 维度 | 评估要点 |
|------|----------|
| **可读性** | 命名规范、注释质量、代码组织 |
| **可维护性** | 模块化程度、耦合度、复杂度 |
| **正确性** | 逻辑正确、边界处理、错误处理 |

### 评分等级（1-10 分）

| 分数 | 等级 | 含义 |
|------|------|------|
| 9-10 | 卓越 | 业界领先，可作为标杆 |
| 7-8 | 良好 | 质量较高，少量优化空间 |
| 5-6 | 一般 | 基本可用，有明显改进空间 |
| 3-4 | 较差 | 存在较多问题，需要重构 |
| 1-2 | 需重构 | 严重影响项目，必须优先处理 |

### 评分要求

- 基于代码实际情况，有据可依
- 各维度独立评分，低分维度必须提供具体改进建议
- 综合评分 = 各维度评分的平均值

### 评分表格式

```markdown
| 维度 | 评分(1-10) | 说明 |
|------|-----------|------|
| {维度} | x | {说明} |

**综合评分：x.x/10**
```

## 7. 常用表格格式

**问题表：**

```markdown
| 严重程度 | 问题 | 位置 | 建议 |
|---------|------|------|------|
| 高/中/低 | {desc} | `[位置](path:line)` | {suggestion} |
```

**属性表：**

```markdown
| 属性 | 值 |
|------|-----|
| 名称 | {value} |
| 路径 | `{path}` |
```

## 8. 图表规范

使用 ASCII 艺术：

```
[顶层]
  ├── [子层1]
  │     ├── [子子层1]
  │     └── [子子层2]
  └── [子层2]
```

依赖方向用箭头：`[模块A] ──▶ [模块B]`
